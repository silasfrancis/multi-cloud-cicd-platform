apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-store
spec:
  provider:
    vault:
      server: "http://<vault-public-ip>:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: {{ .Values.vault_mountPath }}
          role: {{ .Values.vault_role }}
          serviceAccountRef:
            name: eso-service-account
            namespace: {{ .Values.namespace }}