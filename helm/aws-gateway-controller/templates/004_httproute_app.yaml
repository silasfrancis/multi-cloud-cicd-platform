apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app-httproute
  namespace: {{ .Values.apps.namespace }}
spec:
  hostnames:
    -  {{ .Values.http_route.hostnames.app }}
  parentRefs:
    - group: gateway.k8s.aws
      name: {{ .Values.gateway.name }}
      kind: Gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ .Values.apps.react_recoil.service }}
          port: 80
