terraform {
  required_providers {
    linode = {
      source = "linode/linode"
      version = "3.8.0"
    }
  }
}

resource "linode_vpc" "vpc_dev" {
    label = var.vpc_name
    region = var.region
    description = "VPC for test clusters"
  
}